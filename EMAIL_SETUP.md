# 📧 Налаштування автоматичної відправки результатів на Email

Цей проект використовує **Resend** для автоматичної відправки PDF результатів на email адміністратора після завершення оцінювання.

## 🚀 Крок 1: Реєстрація на Resend

1. Перейдіть на https://resend.com
2. Натисніть "Sign Up" (Зареєструватися)
3. Створіть безкоштовний акаунт
4. Підтвердіть email адресу

## 🔑 Крок 2: Отримання API ключа

1. Увійдіть в свій Resend акаунт
2. Перейдіть в розділ **"API Keys"** (https://resend.com/api-keys)
3. Натисніть **"Create API Key"**
4. Дайте назву ключу, наприклад: `youth-centers-production`
5. Скопіюйте згенерований API ключ (він виглядає як `re_...`)
6. ⚠️ **ВАЖЛИВО**: Збережіть ключ в безпечному місці - він показується тільки один раз!

## 🌐 Крок 3: Налаштування Vercel

### 3.1. Створення проекту на Vercel

1. Перейдіть на https://vercel.com
2. Натисніть "Sign Up" і зареєструйтеся (рекомендую через GitHub)
3. Натисніть "Add New Project"
4. Імпортуйте ваш Git репозиторій (GitHub/GitLab/Bitbucket)
5. Vercel автоматично визначить, що це Vite проект

### 3.2. Додавання Environment Variables

Після створення проекту:

1. Перейдіть в розділ **Settings** → **Environment Variables**
2. Додайте **2 змінні**:

   **Змінна 1: RESEND_API_KEY**
   - Name: `RESEND_API_KEY`
   - Value: Вставте ваш API ключ з Resend (починається з `re_...`)
   - Environment: Production, Preview, Development (виберіть всі)

   **Змінна 2: ADMIN_EMAIL**
   - Name: `ADMIN_EMAIL`
   - Value: `zvoryhin.hd@gmail.com` (або інша email адреса адміністратора)
   - Environment: Production, Preview, Development (виберіть всі)

3. Натисніть **"Save"**

### 3.3. Редеплой після додавання змінних

1. Перейдіть в розділ **Deployments**
2. Знайдіть останній deployment
3. Натисніть три крапки (...) → **"Redeploy"**
4. Виберіть "Use existing Build Cache" → **"Redeploy"**

## ✅ Крок 4: Перевірка роботи

1. Відкрийте ваш сайт на Vercel (наприклад, `youth-centers-tool.vercel.app`)
2. Пройдіть оцінювання до кінця
3. На сторінці результатів натисніть кнопку **"📧 Відправити на email"**
4. Дочекайтеся повідомлення про успіх
5. Перевірте email (zvoryhin.hd@gmail.com) - там має бути лист з 2 PDF файлами

## 📊 Безкоштовний план Resend

- **100 emails на день** (3000 на місяць)
- Підтримка вкладень (attachments)
- Відправник: `onboarding@resend.dev` (поки не налаштуєте свій домен)

## 🔄 Зміна email адміністратора

Якщо потрібно змінити email адміністратора або додати декілька:

### Варіант 1: Через Vercel Environment Variables (Рекомендовано)

1. Перейдіть в **Settings** → **Environment Variables**
2. Знайдіть `ADMIN_EMAIL`
3. Натисніть **"Edit"**
4. Змініть значення на новий email
5. Натисніть **"Save"**
6. Зробіть **Redeploy**

### Варіант 2: Додати декілька email адрес

Відкрийте файл `api/send-results.js` і змініть рядок:

```javascript
// Було:
to: [adminEmail],

// Стало (декілька email):
to: [adminEmail, 'another@email.com', 'third@email.com'],
```

Потім зробіть commit і push - Vercel автоматично задеплоїть.

## 🔧 Налаштування власного домену (Опціонально)

Зараз відправник показується як `onboarding@resend.dev`. Щоб використовувати свій домен:

1. В Resend перейдіть в **"Domains"**
2. Додайте ваш домен (наприклад, `undp-ukraine.org`)
3. Налаштуйте DNS записи (Resend покаже інструкції)
4. Після верифікації змініть в `api/send-results.js`:

```javascript
from: 'UNDP Youth Centers <noreply@your-domain.com>',
```

## ❓ Troubleshooting

### Помилка: "Method not allowed"
- Перевірте що Vercel Function задеплоєна правильно
- Перевірте в розділі **Functions** на Vercel

### Помилка: "Failed to send email"
- Перевірте що `RESEND_API_KEY` додано правильно
- Перевірте що ключ активний в Resend
- Подивіться логи в Vercel: **Deployments** → Select deployment → **Function Logs**

### Email не приходить
- Перевірте папку "Спам"
- Перевірте що `ADMIN_EMAIL` налаштовано правильно
- Подивіться логи в Resend Dashboard → **Emails**

### Дуже великі PDF файли
- Зменшіть `scale` в `pdfGenerator.js` (зараз 2, можна змінити на 1.5)
- Зменшіть `quality` зображень (зараз 0.95, можна 0.85)

## 📝 Примітки

- PDF файли генеруються як base64 в браузері користувача
- Розмір одного email може бути до **10MB** (Resend ліміт)
- Типові PDF файли: 500KB - 2MB кожен
- Час відправки: 5-15 секунд (залежить від розміру PDF)

## 💰 Ціни (якщо вийдете за безкоштовний ліміт)

Resend:
- Безкоштовно: 3000 emails/місяць
- Pro: $20/місяць - 50,000 emails
- Business: Custom pricing

Для 500 відповідей на місяць - **безкоштовного плану більш ніж достатньо**!

---

**Остання оновлення:** 2026-01-13
**Контакт для питань:** zvoryhin.hd@gmail.com
